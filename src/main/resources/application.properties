spring.application.name=MQ-Helpers

# Server Configuration
server.port=9092

# Security Configuration
app.security.userId=userName
app.security.password=qx0gYnkY1nFJpjXFVKN7KQ==

# Mock Response Configuration
app.mock.isMockResponse=true

# = DataSource Configuration
spring.datasource.url=jdbc:postgresql://34.18.104.74:5432/dukhanbank
spring.datasource.username=postgres
spring.datasource.password=Poclab_123#
spring.datasource.driver-class-name=org.postgresql.Driver

# = JPA / Hibernate
# Show SQL queries in console
spring.jpa.show-sql=true
# Format SQL output nicely
spring.jpa.properties.hibernate.format_sql=true
# Automatically create/update schema (use update/create-drop for dev, validate for prod)
spring.jpa.hibernate.ddl-auto=update
# PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Rate calculation configuration
app.rate.digits=6

# MQ Configuration
app.mq.queueManager=BBAX013U
app.mq.host=10.200.125.242
app.mq.port=1516
app.mq.channel=MOBILE.CLIENT.CHL
app.mq.requestQueue=GW.MOBILE.REQ
app.mq.replyQueue=GW.MOBILE.REPLY
app.mq.userId=userName
app.mq.password=qx0gYnkY1nFJpjXFVKN7KQ==
app.mq.timeout=30000

# Caching (defaults to in-memory; project runs without Redis)
spring.cache.type=simple

# Redis configuration (optional; uncomment to enable Redis cache)
# spring.cache.type=redis
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.data.redis.password=